# GitOps source for ArgoCD (clearledger-infra). CI updates image tags here only.
# Deployments use placeholders (clearledger/*:gitops); Kustomize maps to registry + SHA.
#
# NOTE: this file's images: section is a template only — CI's update-manifests job keeps the
# real, current newTag in clearledger-infra's copy. Never overwrite that copy with this one.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clearledger

resources:
  - namespace.yaml
  - ingress.yaml
  - auth-service/deployment.yaml
  - auth-service/service.yaml
  - ledger-service/deployment.yaml
  - ledger-service/service.yaml
  - notification-service/deployment.yaml
  - notification-service/service.yaml
  - frontend/deployment.yaml
  - postgres/postgres.yaml
  - postgres/postgres-secret.yaml
  - redis/redis.yaml
  - rbac/rbac.yaml
  - vault/rotation-cronjob.yaml

images:
  - name: clearledger/auth-service
    newName: docker.io/YOUR_DOCKERHUB_USERNAME/clearledger-auth-service
    newTag: v0.1.0
  - name: clearledger/ledger-service
    newName: docker.io/YOUR_DOCKERHUB_USERNAME/clearledger-ledger-service
    newTag: v0.1.0
  - name: clearledger/notification-service
    newName: docker.io/YOUR_DOCKERHUB_USERNAME/clearledger-notification-service
    newTag: v0.1.0
  - name: clearledger/frontend
    newName: docker.io/YOUR_DOCKERHUB_USERNAME/clearledger-frontend
    newTag: v0.1.0
