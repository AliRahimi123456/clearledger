# INTRODUCED: Stage 6.5 — LitmusChaos install
#
# Automated install (recommended):
#   bash stages/stage-6.5-chaos-engineering/scripts/install-litmus.sh
#
# Installs: litmus namespace, RBAC, litmus-core (operator), kubernetes-chaos
# (experiments), ChaosCenter UI (chaos helm release), and litmus-ingress.yaml.
apiVersion: v1
kind: Namespace
metadata:
  name: litmus
