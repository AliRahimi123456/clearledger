# Reference: all three ClearLedger chaos experiments (apply ONE file at a time)
# Split manifests (used by run-chaos.sh and manual runs):
#   auth-service-pod-delete.yaml          — Experiment 1 (automated demo)
#   ledger-service-network-latency.yaml   — Experiment 2 (manual)
#   notification-service-memory-hog.yaml    — Experiment 3 (manual)
#
# ChaosEngine resources live in namespace litmus (not clearledger).
# Kyverno admission policies in clearledger block Litmus runner pods.
# spec.appinfo.appns targets workloads in clearledger.
#
# Or run the guided demo:
#   make demo-65
#   bash stages/stage-6.5-chaos-engineering/scripts/run-chaos.sh
